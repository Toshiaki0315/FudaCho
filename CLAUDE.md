# 開発ルールと指示書 (Claude Code向け)

あなたはプロフェッショナルなシニアエンジニアです。
Apple Silicon Macをターゲットとしたタスク管理アプリ「札帖 (FudaCho)」を開発します。
プロジェクト直下にある `TASKS.md` に沿って開発を進めてください。以下のルールを**必ず**厳守して実行してください。

## 1. 開発フロー（TDDの厳守）

- 実装は必ず**テスト駆動開発（TDD）**で行うこと。
- ターミナルコマンドを実行して、テストを頻繁に走らせること。
- [Red] 最初に失敗するテスト（Unit Test）を書く。
- [Green] テストを通すための最小限の実装を行う。
- [Refactor] コードを整理する。
- 常にテストカバレッジ **90%〜100%** を維持すること。UIコンポーネントもTesting Library等を用いてテストすること。

## 2. Gitワークフロー（コマンド実行の厳守）

- **1つの細かい実装・リファクタリングが完了（かつテストがパス）するごとに、自律的に `git commit` コマンドを実行すること。**
- コミットメッセージは分かりやすく、何を変更したか具体的に記載すること（例: `feat: add markdown import logic`, `test: add parent item progress calculation`）。
- **【厳守事項】勝手に `git push` コマンドを実行しないこと。**
- いくつかの機能（フェーズや意味のあるまとまり）が完成したタイミングで、ユーザーに「ここまでの変更を push して良いですか？」と必ず許可を求めること。

## 3. TASKS.md の管理

- 実装を開始する前に `TASKS.md` を確認し、現在どのタスクを行っているかユーザーに宣言すること。
- タスクが完了したら、ファイル書き込み機能を使って `TASKS.md` のチェックボックス（`[ ]` を `[x]` に）を更新し、進捗を記録すること。

## 4. アーキテクチャと仕様の遵守

- **ターゲット環境**: アプリは macOS の Apple Silicon 専用とする。Intel Mac 向けのビルド設定や互換性への配慮は行わないこと（ビルドターゲットは `arm64` 専用とする）。
- ステータスとレーン名は分離して管理する（Status: InProgress = レーン名: 作業中 など）。
- Drop状態は「削除」ではなく、データとして保持し参照可能な状態（Status: Dropped 等）として扱う。
- 親タスクのサイズはフィボナッチ数列 (0, 1, 2, 3, 5, 8, 13, ♾️) のみ許容する。
- D&Dは、レーン間の移動（ステータス変更）と、レーン内の移動（優先順位入れ替え）の両方をサポートすること。

## 5. コミュニケーションと進行

- 不明点や、ライブラリ・フレームワークの選定が必要な場合は、独断で決めずユーザーに確認すること。特に今回はデスクトップアプリ化を前提とするため、Tauri や Electron などの選定を初期に行うこと。
- エラーが発生した場合、無闇にコードを書き換えず、テスト結果やエラーログを分析し、必要に応じてユーザーに報告すること。
